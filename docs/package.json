{
  "name": "@neptu/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name=neptu-docs --branch=main --commit-dirty=true"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "^3.114.17"
  }
}
